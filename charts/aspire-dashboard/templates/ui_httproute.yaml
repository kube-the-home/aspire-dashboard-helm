{{- $name := include "app.name" . -}}

{{- if .Values.ui.route.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- if .Values.ui.route.labels }}
    {{- toYaml .Values.ui.route.labels | nindent 4 }}
    {{- end }}
  {{- if .Values.ui.route.annotations }}
  annotations:
    {{- toYaml .Values.ui.route.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ui.route.parentRefs }}
  parentRefs:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.ui.route.hostnames }}
  hostnames:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ $name }}-ui
          port: {{ .Values.ui.port }}
{{- end }}
